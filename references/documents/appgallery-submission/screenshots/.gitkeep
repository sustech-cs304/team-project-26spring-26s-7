真机截图放这里，命名建议（4 张全部必交，命名跟下面对齐方便审核员看）：

- 01-aicopy-page.jpg          AiCopyPage 生成完文案，黄底警示 "⚠ 本段文字由 AI 生成" 可见
- 02-nodeedit-after-apply.jpg 应用到 NodeEditPage 后正文 TextArea 最开头有 "【AI生成】"
- 03-nodedetail-display.jpg   NodeDetailPage 详情正文区域以 "【AI生成】" 开头
- 04-share-viewer.jpg         浏览器打开分享 URL 的 HTML 节点卡片同样以 "【AI生成】" 开头

格式要求（AGC 上传栏字面要求）：
- 必须 JPG / JPEG / BMP（PNG 不收，HMOS 截屏默认 PNG 要先转 JPG）
- 单张 ≤ AGC 默认 size，4 张装 zip 一起传

转 PNG → JPG（在你的本地电脑上跑，需要 ImageMagick）：
  for f in *.png; do convert "$f" "${f%.png}.jpg"; done
